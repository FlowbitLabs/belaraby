To create a new supabase migration using the following command

```bash
supabase migration new 'migration_name'
```

then add your sql migration. Afterward push it to dev branch and it will auto-deploy
